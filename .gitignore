# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node / Bun
node_modules/
.next/
out/
.bun/
*.log
*.tsbuildinfo
frontend/playwright-report/
frontend/test-results/
# NOTE: bun.lock IS committed (frozen-lockfile reproducibility + SBOM
# generation depend on it). Do not gitignore.

# Foundry
contracts/out/
contracts/cache/

# OS
.DS_Store
Thumbs.db

# Project state
backend/data_store/*.db
backend/data_store/*.db-journal
backend/data_store/*.db-wal
backend/data_store/*.db-shm
backend/data_store/contract_address.json
backend/app/fraud/training/*.joblib
.env
.env.local
*.local
.claude/

# Secrets — never committed under any circumstance. The single permitted
# exception is the public PGP key at docs/security/landguard-maintainer.asc.
*.private.asc
*.private.gpg
*-revoke-*.asc
*.key
id_rsa
id_ed25519
*.pem
*credentials*
*token*

# Local docker-compose override (host-specific port remaps, user pinning).
# Canonical compose stays clean; local divergence is gitignored.
docker-compose.override.yml

# Crane Cloud per-environment values (templates *.env.example are committed)
infra/cranecloud/environments/*.env

# Evidence artefacts — SBOMs are committed (dated submission artefact);
# perf/accessibility runs are per-machine and uploaded as CI artefacts instead.
evidence/lighthouse/
evidence/load/
evidence/probes/
